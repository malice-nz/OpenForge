fn main() {
    eprintln!("openforge-gui: scaffold only. Use the openforge CLI for now.");
    eprintln!("Plan: egui-based native GUI with search, install queue, instance manager.");
    std::process::exit(2);
}
